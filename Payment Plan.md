
### 💡 خطة الدفع المتوافقة مع سياسات جوجل:

## 🛠️ الخطة المعدلة (نظام التجربة الصامتة - Backend Trial)

### 1. Database (Supabase)

#### [NEW] Database Trigger: `on_user_created_grant_trial`
سنعيد استخدام فكرتك الأولى الممتازة (ولكن بدون فواتشرز).
- **الحدث:** عند إنشاء حساب جديد في `auth.users`.
- **الإجراء:** يقوم الـ Trigger بإنشاء صف للمستخدم في جدول `user_subscriptions`، ويجعل حقل `expires_at` يساوي تاريخ اليوم + 7 أيام (`NOW() + INTERVAL '7 days'`).

#### [MODIFY] `get_localized_lessons` (RPC)
نفس المنطق السابق بالضبط:
- الدالة تنظر لحقل `expires_at`. ما دام التاريخ في المستقبل (سواء كانت فترة الـ 7 أيام المجانية أو اشتراك مدفوع لاحقاً)، سيتم إرجاع روابط الملفات.
- إذا انتهت الـ 7 أيام، تعود الروابط بـ `NULL`.

---

### 2. Backend Integration (RevenueCat & Stores)

- **في لوحة تحكم المتاجر (Google / Apple):** قم بإنشاء الاشتراكات (شهري/سنوي) **بدون تفعيل خيار Free Trial نهائياً**.
- **الـ Webhooks:** سيظل RevenueCat يرسل تحديثات إلى Supabase. إذا قرر المستخدم الدفع بعد انتهاء الأسبوع التجريبي، سيقوم الـ Webhook بتحديث حقل `expires_at` في Supabase ليمتد شهراً أو سنة حسب الباقة.

---

### 3. App (Flutter Logic & UI)

#### [MODIFY] `SubscriptionCubit`
الآن الـ Cubit سيعتمد على **Supabase** كمرجع أساسي:
1. عند الدخول، يجلب تاريخ `expires_at` من جدول `user_subscriptions`.
2. إذا كان التاريخ صالحاً (أكبر من تاريخ اليوم) -> `isVip = true` (المستخدم يستمتع بالأسبوع المجاني أو هو دافع بالفعل).
3. إذا انتهى التاريخ -> `isVip = false` (تظهر الأقفال على الدروس).

#### [MODIFY] `PaywallScreen` (UI)
- تظهر **فقط** عندما يضغط المستخدم على درس مقفول (أي بعد انتهاء الـ 7 أيام).
- يمكنك إضافة رسالة ذكية في هذه الشاشة مثل: *"انتهت فترة تجربتك المجانية، اشترك الآن لمواصلة التعلم"*.
- عند الشراء، يتم استخدام RevenueCat ليظهر نافذة الدفع، ويتم الخصم فوراً (لأنه لا يوجد Free Trial في المتجر).

---

### 🌟 مميزات هذا النظام:
1. **لا يوجد إدخال لبيانات الدفع في البداية:** المستخدم يحمل التطبيق، يسجل الدخول، ويبدأ الاستماع فوراً.
2. **أمان تام:** الروابط تظل محمية بـ RPC بناءً على تاريخ الصلاحية.
3. **متوافق مع المتاجر:** أنت لا تلتف على الدفع، أنت فقط قررت أن تجعل تطبيقك مجانياً بالكامل لأول 7 أيام من طرفك، وهذا حقك.
4. **دقة البيانات:** قاعدة بياناتك (Supabase) هي "مصدر الحقيقة" الوحيد (Single Source of Truth) لحالة المستخدم، سواء كان في فترته المجانية أو دفع لاحقاً.

بهذه الطريقة، تضمن أعلى نسبة تسجيل مستخدمين جدد، وتجربة سلسة جداً، مع حماية محتواك وأموالك من مشاكل المتاجر!
